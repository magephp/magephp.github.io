language: ruby
cache: bundler
sudo: false
rvm: 2.1
exclude: [vendor]
before_script: bundle install --no-deployment
script: bundle exec jekyll build
branches:
  only:
    - gh-pages
notifications:
  email:
    on_success: never
    on_failure: never
