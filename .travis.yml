language: ruby
rvm: 2.1
sudo: false
cache: bundler
addons:
addons:
  apt:
    packages:
    - curl
install:
  - bundle install --no-deployment
script:
  - bundle exec jekyll build
branches:
  only:
    - gh-pages
notifications:
  email:
    on_success: never
    on_failure: never
